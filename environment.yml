name: garmin-ai-coach
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - nodejs
  - pip:
      - black
      - fastapi
      - garminconnect
      - httpx
      - openai
      - pydantic
      - pypdf
      - pylint
      - pytest
      - python-dotenv
      - python-multipart
      - uvicorn
