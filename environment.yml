name: garmin-ai-coach
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - nodejs
  - pip:
      - black
      - cryptography
      - fastapi
      - garminconnect
      - httpx
      - openai
      - pydantic
      - pypdf
      - pylint
      - playwright
      - pymysql
      - pytest
      - python-dotenv
      - python-multipart
      - sqlalchemy
      - uvicorn
